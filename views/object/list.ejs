<%
var AtoZ = {};
var sortedObjects = [];

for(t in objects){
    var fun = objects[t];
    var tagInitial = t;
    if(!AtoZ[tagInitial]){
        AtoZ[tagInitial] = [];
        sortedObjects.push(tagInitial);
    }
    AtoZ[tagInitial].push(fun);

}
    sortedObjects.sort();
%>


<section id="tags">
    <div class="page-header">
        <h2>Objects</h2>
        <p class="lead">Lucee Server provides you with a number of objects that hold collections and that have functions that you can call. These are normally covered by funcstions such as <code>ArrayContains(MyArray)</code> but member functions allow you to call <code>MyArray.contains()</code> instead for brevity.</p>

            <div class="row">
                <div class="col-md-12">
                    <ul class="nav nav-justified">
                        <% for(name in sortedObjects) { %>
                        <li class="btn btn-default btn-xs"><a href="<%= "#" + sortedObjects[name] %>"><%= sortedObjects[name] %></a></li>
                        <% } %>

                    </ul>
                </div>
            </div>
    </div>

    <div class="row">
        <% for(name in sortedObjects) {

            var ObjectName = sortedObjects[name];
            var ObjectFuncs = AtoZ[ObjectName];
        %>
        <div id="<%= ObjectName %>" class="anchor col-sm-4">
            <h4><%= ObjectName.toUpperCase() %></h4>
            <ul class="list list-unstyled">
                <% for(tagItem in objects[ObjectName]) {%>
                <li><a href="/object/<%= ObjectName%>/<%= tagItem %>"><%= tagItem %>


                    </a>


                </li>
                    <% } %>
             </ul>
          </div>
        <% } %>
    </div>

</section>


